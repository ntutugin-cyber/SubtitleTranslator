<Window
    x:Class="SubtitleTranslator.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="clr-namespace:SubtitleTranslator.ViewModels"
    Title="Переводчик субтитров"
    Width="1200"
    Height="750"
    Background="#1E1E1E"
    WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <!--  Тёмная тема  -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="#0078D4" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="20,10" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1084DE" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#005A9E" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#3F3F3F" />
                                <Setter Property="Foreground" Value="#7F7F7F" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#E0E0E0" />
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2D2D30" />
            <Setter Property="Foreground" Value="#E0E0E0" />
            <Setter Property="BorderBrush" Value="#3F3F3F" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10" />
            <Setter Property="FontFamily" Value="Consolas" />
            <Setter Property="FontSize" Value="13" />
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#E0E0E0" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Margin" Value="0,0,20,0" />
        </Style>

        <Style TargetType="ProgressBar">
            <Setter Property="Height" Value="8" />
            <Setter Property="Background" Value="#3F3F3F" />
            <Setter Property="Foreground" Value="#0078D4" />
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="Background" Value="#2D2D30" />
            <Setter Property="Foreground" Value="#E0E0E0" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Padding" Value="15,8" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border
                            Margin="0,0,2,0"
                            Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            CornerRadius="8,8,0,0">
                            <ContentPresenter
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                ContentSource="Header" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#0078D4" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#3F3F46" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TabControl">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  Заголовок  -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock
                FontSize="20"
                FontWeight="Bold"
                Foreground="#0078D4"
                Text="🎬 Переводчик субтитров" />
            <TextBlock
                Margin="0,5,0,0"
                FontSize="14"
                Foreground="#9E9E9E"
                Text="JSON → SRT с нейросетевым переводом NLLB-200" />
        </StackPanel>

        <!--  TabControl  -->
        <TabControl Grid.Row="1">
            <!--  Вкладка 1: Файловый режим  -->
            <TabItem Header="📁 Работа с файлами">
                <Grid Margin="0,10,0,0" Background="#252526">
                    <Grid.DataContext>
                        <vm:MainViewModel x:Name="FileViewModel" />
                    </Grid.DataContext>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <!--  Кнопки управления  -->
                    <StackPanel
                        Grid.Row="0"
                        Margin="0,0,0,20"
                        Orientation="Horizontal">
                        <Button
                            Margin="0,0,10,0"
                            Command="{Binding LoadFileCommand}"
                            Content="📂 Загрузить из файла" />
                        <Button
                            Margin="0,0,10,0"
                            Command="{Binding LoadFromTextCommand}"
                            CommandParameter="{Binding ElementName=RawJsonTextBox, Path=Text}"
                            Content="📋 Загрузить из текста" />
                        <Button
                            Margin="0,0,10,0"
                            Command="{Binding TranslateCommand}"
                            Content="🌐 Перевести" />
                        <Button Command="{Binding SaveSrtCommand}" Content="💾 Сохранить SRT" />
                    </StackPanel>

                    <!--  Таблица субтитров  -->
                    <Border
                        Grid.Row="1"
                        Background="#252526"
                        BorderBrush="#3F3F3F"
                        BorderThickness="1"
                        CornerRadius="10">
                        <DataGrid
                            AlternatingRowBackground="#2D2D30"
                            AutoGenerateColumns="False"
                            Background="Transparent"
                            BorderThickness="0"
                            CanUserAddRows="False"
                            CanUserDeleteRows="False"
                            Foreground="#E0E0E0"
                            GridLinesVisibility="Horizontal"
                            HeadersVisibility="Column"
                            HorizontalGridLinesBrush="#3F3F3F"
                            IsReadOnly="True"
                            ItemsSource="{Binding Subtitles}"
                            RowBackground="#252526">
                            <DataGrid.Columns>
                                <DataGridTextColumn
                                    Width="50"
                                    Binding="{Binding Speaker}"
                                    Header="#" />
                                <DataGridTextColumn Width="150" Header="Время">
                                    <DataGridTextColumn.Binding>
                                        <MultiBinding StringFormat="{}{0:F1} - {1:F1}">
                                            <Binding Path="Start" />
                                            <Binding Path="End" />
                                        </MultiBinding>
                                    </DataGridTextColumn.Binding>
                                </DataGridTextColumn>
                                <DataGridTextColumn
                                    Width="*"
                                    Binding="{Binding Content}"
                                    Header="Оригинал" />
                                <DataGridTextColumn
                                    Width="*"
                                    Binding="{Binding TranslatedContent}"
                                    Header="Перевод" />
                                <DataGridTextColumn
                                    Width="100"
                                    Binding="{Binding DetectedLang}"
                                    Header="Язык" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>

                    <!--  Прогресс и статус  -->
                    <StackPanel Grid.Row="2" Margin="0,20,0,0">
                        <ProgressBar
                            Maximum="100"
                            Visibility="{Binding IsProcessing, Converter={StaticResource BooleanToVisibilityConverter}}"
                            Value="{Binding Progress}" />
                        <TextBlock
                            Margin="0,10,0,0"
                            FontSize="12"
                            Foreground="#9E9E9E"
                            Text="{Binding StatusText}" />
                    </StackPanel>
                </Grid>
            </TabItem>

            <!--  Вкладка 2: Работа с сырым JSON  -->
            <TabItem Header="✏️ Работа с текстом">
                <Grid Margin="0,10,0,0" Background="#252526">
                    <Grid.DataContext>
                        <vm:RawJsonViewModel />
                    </Grid.DataContext>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <!--  Панель управления  -->
                    <StackPanel Grid.Row="0" Margin="0,0,0,20">
                        <!--  Опции  -->
                        <StackPanel Margin="0,0,0,15" Orientation="Horizontal">
                            <CheckBox Content="🌐 Переводить на русский" IsChecked="{Binding EnableTranslation}" />
                            <CheckBox Content="👤 Указывать говорящих" IsChecked="{Binding IncludeSpeakers}" />
                            <CheckBox
                                Content="Не больше 2"
                                IsChecked="{Binding NoMoreThanTwo}"
                                ToolTip="Указывать говорящих, но их количество не больше двух" />
                            <CheckBox
                                Content="Указывать звуки"
                                IsChecked="{Binding IncludeSounds}"
                                ToolTip="Указывать посторонние звуки (музыку, звуки окружения и т. д.)" />
                        </StackPanel>

                        <!--  Кнопки  -->
                        <StackPanel Orientation="Horizontal">
                            <Button
                                Margin="0,0,10,0"
                                Command="{Binding ConvertToSubtitlesCommand}"
                                Content="📝 В субтитры (SRT)" />
                            <Button
                                Margin="0,0,10,0"
                                Command="{Binding ConvertToTextCommand}"
                                Content="📄 В текст" />
                            <Button
                                Background="#D83B01"
                                Command="{Binding ClearAllCommand}"
                                Content="🗑️ Очистить" />
                        </StackPanel>
                    </StackPanel>

                    <!--  Текстовые поля  -->
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <!--  Входной JSON  -->
                        <Border
                            Grid.Column="0"
                            Margin="0,0,10,0"
                            Background="#2D2D30"
                            BorderBrush="#3F3F3F"
                            BorderThickness="1"
                            CornerRadius="10">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>

                                <TextBlock
                                    Grid.Row="0"
                                    Margin="15,15,15,10"
                                    FontSize="14"
                                    FontWeight="SemiBold"
                                    Text="📥 Входной JSON" />

                                <TextBox
                                    x:Name="RawJsonTextBox"
                                    Grid.Row="1"
                                    Margin="5"
                                    AcceptsReturn="True"
                                    HorizontalScrollBarVisibility="Auto"
                                    Text="{Binding RawJsonText, UpdateSourceTrigger=PropertyChanged}"
                                    TextWrapping="Wrap"
                                    VerticalScrollBarVisibility="Auto" />
                            </Grid>
                        </Border>

                        <!--  Результат  -->
                        <Border
                            Grid.Column="1"
                            Margin="10,0,0,0"
                            Background="#2D2D30"
                            BorderBrush="#3F3F3F"
                            BorderThickness="1"
                            CornerRadius="10">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>

                                <TextBlock
                                    Grid.Row="0"
                                    Margin="15,15,15,10"
                                    FontSize="14"
                                    FontWeight="SemiBold"
                                    Text="📤 Результат" />

                                <TextBox
                                    Grid.Row="1"
                                    Margin="5"
                                    AcceptsReturn="True"
                                    HorizontalScrollBarVisibility="Auto"
                                    IsReadOnly="True"
                                    Text="{Binding ResultText, Mode=OneWay}"
                                    TextWrapping="Wrap"
                                    VerticalScrollBarVisibility="Auto" />
                            </Grid>
                        </Border>
                    </Grid>

                    <!--  Статус  -->
                    <StackPanel Grid.Row="2" Margin="0,20,0,0">
                        <TextBlock
                            FontSize="12"
                            Foreground="#9E9E9E"
                            Text="{Binding StatusText}" />
                    </StackPanel>
                </Grid>
            </TabItem>
        </TabControl>

        <TextBox
            x:Name="LogTextBox"
            Grid.Row="2"
            MaxHeight="150"
            Margin="5"
            AcceptsReturn="True"
            HorizontalScrollBarVisibility="Auto"
            Text="{Binding LogText, UpdateSourceTrigger=PropertyChanged}"
            TextWrapping="Wrap"
            VerticalScrollBarVisibility="Auto" />
    </Grid>
</Window>